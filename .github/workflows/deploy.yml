name: Deploy Docs

on:
  workflow_run:
    workflows: [Build]
    types: [completed]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy Docs To Server
        run: curl ${{ secrets.SERVER_UPDATE_URL }}
